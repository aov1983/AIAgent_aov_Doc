# Базовый URL n8n-вебхуков, вшивается в бандл на сборке (Vite: import.meta.env.VITE_API_URL).
# Прод (Docker за nginx): относительный префикс /webhook — фронт ходит на свой origin.
# Dev-режим (npm run dev) против локального n8n:
VITE_API_URL=http://localhost:5678/webhook
